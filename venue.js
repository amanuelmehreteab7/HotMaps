class Venue {
  constructor(name, cat, lat, lng, address, venueId, url, hereNow, checkinsCount, categoryId) {
    this.name = name;
    this.cat = cat;
    this.lat = lat;
    this.lng = lng;
    this.address = address;
    this.venueId = venueId;
    this.url = url;
    this.hereNow = hereNow;
    this.checkinsCount = checkinsCount;
    this.categoryId = categoryId;
  }
}
